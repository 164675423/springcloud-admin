logging:
  level:
    root: info
    com.zh.storage: debug
spring:
  profiles:
    active: dev
  application:
    name: fileStorage
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml