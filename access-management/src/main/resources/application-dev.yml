server:
  port: 9091
spring:
  datasource:
    url: jdbc:mysql://rm-2zefmq87x5r426x16uo.mysql.rds.aliyuncs.com:3306/access_management?useSSL=false&characterEncoding=UTF-8
    username: am
    password: am2020-2099
    driver-class-name: com.mysql.jdbc.Driver
    tomcat:
      min-idle: 2
      test-while-idle: true
      test-on-borrow: true
      time-between-eviction-runs-millis: 10000
      min-evictable-idle-time-millis: 55000
      validation-interval: 34000
      validation-query: SELECT 1
  redis:
    host: 118.178.254.170
    port: 6379
    database: 0 # Redis数据库索引（默认为0）
    password: # Redis连接密码
    lettuce:
      pool:
        max-idle: 8 # 连接池中的最大空闲连接
        min-idle: 0 # 连接池中的最小空闲连接
        max-active: 8 # 连接池最大连接数（使用负值表示没有限制）
        max-wait: -1ms # 连接池最大阻塞等待时间（使用负值表示没有限制）
  kafka:
    bootstrap-servers: 111.229.165.30:9092
    consumer:
      group-id: am
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      enable-auto-commit: true
    producer:
      retries: 3 # 重试次数
eureka:
  client:
    service-url:
      defaultZone: http://118.178.254.170:8001/eureka/
    registry-fetch-interval-seconds: 3
  instance:
    lease-expiration-duration-in-seconds: 9
    lease-renewal-interval-in-seconds: 3
management:
  endpoints:
    web:
      exposure:
        include: '*'
application:
  kafka:
    topics:
      user: dev-am-user
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 60000
        loggerLevel: full
